# frozen_string_literal: true

RSpec.describe FactoryBot::With do
  it "has a version number" do
    expect(FactoryBot::With::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
